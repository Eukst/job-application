<h3 class="text-center">Create New Job</h3>
<% if user_signed_in?%>
<% if current_user.user_role.include?"Employer"%>

<%= render 'form', job: @job %>

<%end%>
<%end%>
<%if current_user.user_role.include?"Employer"%>
   <%= link_to 'Show All Jobs', root_path , class:"btn btn-info"%>
<%else%>
    <%= link_to 'Show All Jobs', jobs_path ,class:"btn  btn-info"%>
<%end%>
