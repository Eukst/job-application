<%= form_with( url: apply_jobs_path, remote: true) do |form| %>
  <%= hidden_field_tag :user_id,current_user.id %>
   <%= hidden_field_tag :job_id , @job.id%>
<%= form.submit "Apply To Job " , class:"btn btn-primary"%> 
<% end %>
